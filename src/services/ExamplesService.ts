/**
 * ExamplesService - Provides access to additional word examples.
 * Currently a stub (no extended examples data bundled).
 */

/**
 * Get additional examples for a word.
 * Returns null if no examples exist.
 */
export function getExamples(_wordId: string): string[] | null {
  return null;
}

/**
 * Check if a word has additional examples.
 */
export function hasExamples(_wordId: string): boolean {
  return false;
}
